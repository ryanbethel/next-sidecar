{
  "name": "next-sidecar",
  "version": "0.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "NODE_ENV=production next build && cp -r .next ../dist/ "
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-next": "^12.0.2"
  }
}
