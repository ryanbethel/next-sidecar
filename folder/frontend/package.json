{
  "name": "next-sidecar",
  "version": "0.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "NODE_ENV=production next build && cp -r .next ../dist/ "
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
